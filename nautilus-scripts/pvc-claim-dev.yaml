apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: waymo-dev
  namespace: aiea-slugbotics
spec:
  storageClassName: rook-cephfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: <INSERT_STORAGE_SIZE>
